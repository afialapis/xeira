{
  "name": "browser-pkg-react",
  "main": "lib/index.js",
  "cjs": "dist/browser-pkg-react.cjs.js",
  "browser": "dist/browser-pkg-react.umd.js",
  "module": "dist/browser-pkg-react.es.js",  
  "scripts": {
    "make_link": "mkdir -p node_modules && cd node_modules && rm -fr xeira && ln -fs ../../.. xeira",
    "clean": "rm -fr dist/* && rm -fr lib/*",
    "lint": "npm run make_link && node ../../bin/xeira.mjs lint src",
    "transpile": "npm run make_link && node ../../bin/xeira.mjs transpile src lib",
    "bundle": "node ../../bin/xeira.mjs bundle",
    "all": "npm run clean && npm run lint && npm run transpile && npm run bundle"
  },
  "eslintConfig": {
    "extends": [
      "./node_modules/xeira/configs/eslint.react"
    ]
  },
  "babel": {
    "extends": "./node_modules/xeira/configs/babel.react"
  }
}
